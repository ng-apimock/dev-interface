/** Preset request. */
export class SelectPresetRequest {
    name: string;

    /**
     * Constructor.
     * @param {string} name The mock name.
     */
    constructor(name: string) {
        this.name = name;
    }
}
